services:
  api:
    build: 
      context: .
    depends_on:
      - postgres
    ports:
      - 8000:8000
    volumes:
      - ./:/usr/src/app:ro    ## to make read only added :ro
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload      
    # env_file:
    #   - ./.env
    environment:
      DB_USERNAME: postgres
      DB_PASSWORD: ladolado
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: postgres #fastapi_X
      SECRET_KEY: "09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7"
      ALGORITHM: "HS256"
      ACCESS_TOKEN_EXPIRE_MINUTES: 30

  postgres:
    image: postgres
    restart: always
    # env_file:
    #   - ./.env    
    environment:
      POSTGRESS_PASSWORD: ladolado
      POSTGRESS_DB: postgres #fastapi_X
    volumes:
      - postgres-db:/var/lib/postgresql/data

volumes:
      postgres-db:

